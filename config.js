// Paste your Stripe Payment Link URL below once created in the Stripe Dashboard
// (Dashboard > Payment Links > + New). It will look like:
// https://buy.stripe.com/xxxxxxxxxxxx
window.EQUITON_CONFIG = {
  stripePaymentLink: "",
};
